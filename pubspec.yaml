name: restaurants
description: A school assignment about a restaurants app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  cached_network_image: ^3.2.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  geolocator: ^8.2.1
  google_maps_flutter: ^2.1.6
  image_picker: ^0.8.5+3
  path: ^1.8.0
  provider: ^6.0.2
  shared_preferences: ^2.0.15
  sqflite: ^2.0.2+1
  rxdart: ^0.27.4
  http: ^0.13.4
dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
